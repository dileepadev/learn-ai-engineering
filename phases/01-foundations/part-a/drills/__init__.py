"""Your work goes here.

One module per Part A topic. Every function starts as a typed signature, a
docstring explaining the exercise, and `raise NotImplementedError`. Replace the
raise with a real implementation; the matching test in `../tests/` turns green.

Read the lesson in `../lessons/` first — each drill maps to a section of it.
"""
